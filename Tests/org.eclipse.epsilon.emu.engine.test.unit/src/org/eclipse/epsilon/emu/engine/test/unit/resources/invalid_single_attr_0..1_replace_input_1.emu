@action replace
@feature radius
pattern mutation_1
instance:Place
guard: instance.outgoing.size()>=2
{
	do {
		var newRadius:Real = 33.00;
		// Replace with incompatible type
		instance.radius = newRadius;
	}
}
//Done