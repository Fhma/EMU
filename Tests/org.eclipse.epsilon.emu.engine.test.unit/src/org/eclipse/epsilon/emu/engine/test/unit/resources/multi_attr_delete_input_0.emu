@action delete
@feature coordinates
pattern mutation_0
instance:Place
guard: instance.radius.isUndefined()
{
	do {
		var item = instance.coordinates.random();
		instance.coordinates.remove(item);
	}
}
//Done