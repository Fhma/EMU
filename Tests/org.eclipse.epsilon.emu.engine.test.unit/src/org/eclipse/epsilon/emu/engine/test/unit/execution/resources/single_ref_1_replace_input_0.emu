@action replace
@property source
pattern mutation_0
instance:PlaceToTransArc
guard: instance.source.name.equals("P8")
{
	do {
		var newPlace=Place.createInstance();
		newPlace.name="new place";
		instance.source = newPlace;	
	}
}
//Done