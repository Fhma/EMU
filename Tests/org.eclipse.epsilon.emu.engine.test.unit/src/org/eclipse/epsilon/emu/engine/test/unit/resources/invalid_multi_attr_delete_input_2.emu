@action delete
@feature coordinates
pattern mutation_2
instance:Place
guard: instance.fill_colour = "green"
{
	do {
		var point:Integer = 99;
		// delete un-existing item from a collection
		instance.coordinates.remove(point);
	}
}
//Done