//1) MatchedRule element deletion:
@action delete
@feature elements
pattern mutation_operator_1
instance:Module
{
	do {
		var elem = instance.elements.all.random();
		elem.println("to be deleted: ");
		instance.elements.remove(elem);
	}
}