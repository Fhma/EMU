//1) MatchedRule element deletion:
@action delete
@feature elements
@type module
pattern case_1_delete_MatchedRule
module:Module,
rules:Rule from: Rule.all.select(e|e.isKindOf(MatchedRule))
{
	do {
		module.elements.remove(rules);
	}
}
